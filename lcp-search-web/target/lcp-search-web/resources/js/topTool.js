function showBuyerCenter(currSpan) {
	currSpan.parent().children(".menu-bd").show();
}

function hideBuyerCenter(currSpan) {
	currSpan.parent().children(".menu-bd").hide();
}

function showSellerCenter(currSpan) {
	currSpan.parent().children(".menu-bd").show();
}

function hideSellerCenter(currSpan) {
	currSpan.parent().children(".menu-bd").hide();
}

function showCartCenter(currSpan) {
	currSpan.parent().children(".menu-bd").show();
}

function hideCartCenter(currSpan) {
	currSpan.parent().children(".menu-bd").hide();
}

function showFavoriteCenter(currSpan) {
	currSpan.parent().children(".menu-bd").show();
}

function hideFavoriteCenter(currSpan) {
	currSpan.parent().children(".menu-bd").hide();
}